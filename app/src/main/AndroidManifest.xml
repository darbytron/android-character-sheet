<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tylerdarby.charactersheet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".utils.DataManager">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CharacterEditDisplayActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".activities.CharacterDisplayActivity" />
        <activity android:name=".activities.UserRegistration"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".activities.DiceRoller"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".activities.SettingsActivity"/>
        <activity android:name=".activities.FeatsAndStrengths" />
        <activity android:name=".activities.Spells" />
        <activity android:name=".activities.CharacterSelect"
            android:windowSoftInputMode="adjustPan"/>
    </application>

</manifest>